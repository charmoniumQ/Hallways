package com.github.jachan.hallways;

import java.util.Set;

/**
 * Code that actually locates you based on data
 */
public class Locator {
    /**
     * Returns the current location based on matching the fingerprints measured at the client with
     * similar data found on the server
     */
    static Location locate(Set<Fingerprint> clientData, Set<Field> serverData) {
        return null;
    }
}
